spring:
  application:
    name: VideoGames

---

spring:
  config:
    activate:
      on-profile: prod

  datasource:
    url: jdbc:h2:mem:videogames-prod
    driver-class-name: org.h2.Driver
  sql:
    init:
      mode: always
      data-locations: classpath:data.sql
server:
  port: 8095
